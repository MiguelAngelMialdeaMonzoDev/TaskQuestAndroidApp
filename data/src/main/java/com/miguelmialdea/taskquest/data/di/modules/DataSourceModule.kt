package com.miguelmialdea.taskquest.data.di.modules

import org.koin.dsl.module

val dataSourceModule = module {
    /*single<UserRemoteDataSource> {
        UserRemoteDataSourceImpl(
            userApiService = get()
        )
    }

    single<EpicQuestRemoteDataSource> {
        EpicQuestRemoteDataSourceImpl(
            epicQuestApiService = get()
        )
    }

    single<QuestRemoteDataSource> {
        QuestRemoteDataSourceImpl(
            questApiService = get()
        )
    }

    single<UserStatsRemoteDataSource> {
        UserStatsRemoteDataSourceImpl(
            userStatsApiService = get()
        )
    }*/
}